{
  "name": "ai-capella-omr",
  "private": true,
  "type": "module",
  "description": "Sheet-music scanning for AI-Capella: Audiveris recognition, then a GPT check of every page against the scan.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "fflate": "^0.8.2",
    "firebase-admin": "^13.4.0",
    "openai": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.4"
  }
}
